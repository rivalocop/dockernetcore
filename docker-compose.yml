version: "3"

services:
  web:
    build: .
    ports:
      - "3000:80"
    depends_on:
      - sql.data
  sql.data:
    image: "mcr.microsoft.com/mssql/server:2017-latest-ubuntu"
    environment:
      - SA_PASSWORD=Your_password123
      - ACCEPT_EULA=Y